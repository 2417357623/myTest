<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyan.bank.dao.AccountDao">
    <select id="selectByActno" resultType="com.liyan.bank.pojo.Account">
    select
        id,actno,balance
    from t_account
        where actno = #{actno}
    </select>

    <update id="updateByActno" >
        update t_account set balance = #{balance} where actno = #{actno}
    </update>
</mapper>